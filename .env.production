# Production on Vercel — mock mode for client demo (no backend needed)
# When real backend is ready: set VITE_USE_MOCK=false and add VITE_API_URL
VITE_USE_MOCK=true

# Uncomment and set this when switching to real backend:
# VITE_API_URL=https://your-render-app.onrender.com
